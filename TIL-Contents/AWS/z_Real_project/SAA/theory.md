### 저장소
EBS - 하나의 AZ와vpc내 하나의 인스턴스에 적용되는 저장소
EFS - 여러리전와 여러AZ 내 여러 인스턴스에 적용 됨

---

S3 마이그레이션 시  - AWS Snowball 및 AWS Snowball Edge 는 기존 저장소에서 네트워크 대역폭이 충분하지
않을 때, 대용량 데이터 세트를 클라우드로 이전하는데 도움이 됩니다.
Snowball 장치는 80TB, Snowball Edge 는 100TB 까지 한번에 이동 가능합니다.

---

메세지 관련된건 거의 SNS SQS 
- 들어오는 요청을 Amazon SQS 로 라우팅함으로써 회사는 처리 인스턴스에서 작업 요청을
분리할 수 있습니다. 이를 통해 대기열 크기에 따라 인스턴스 수를 확장하여 필요할 때 더
많은 리소스를 제공할 수 있습니다. 또한 대기열 크기를 기반으로 하는 Auto Scaling
그룹을 사용하면 워크로드에 따라 자동으로 인스턴스 수를 늘리거나 줄일 수 있습니다.
대기열에서 읽을 수 있도록 소프트웨어를 업데이트하면 보다 효율적인 방식으로 작업
요청을 처리할 수 있어 시스템 성능이 향상됩니다.
솔루션을 분리 = SQS.

---

- 회복 탄력성 = SQS 대기열 구성

---

### RestAPI 요청 순서대로
= `SQS FIFO`

### 키값을 쓰는데 보안최소화 
= `seceret Manager`

### CloudFront 는 
= 무조건 S3와 ALB 포함  

### VPC 트래픽 관련
= AWS `네트워크 방화벽`을 사용하여 프로덕션 VPC 에 대한 트래픽 검사 및 트래픽
필터링에 필요한 규칙을 생성합니다.

### 데이터레이크
= Amazon QuickSight를 통한 분석과 읽기전용 게시

### 어플라이언스
> 첨들어보는데 | NatGateway와 동일한 방화벽
= `검사 VPC`에 게이트웨이 로드밸런서 앤드포인트를 생성하여 수신 패킷을 수신 후 어플라이언스 실행

### 동일 리전 DB 테스트
= EBS 스냅샷 -> 그 스냅샷 새 볼륨으로 복원

### 시간당 수백만건의 요청을 밀리초단위로 응답 원함
= CloudFront 정적웹사이트 호스팅 + DynamoDB API 호출

### S3 지능형 계층화 = 빙하 유연 검색
> 복원력 + 자주쓰는데 안쓰는것도 있을 때

### ㅊ

### S3 1개월 후 안쓸때 
> standard에서  Glacier Deep Archive로 변경

### 수 많은 VPC
> `전송게이트웨이`+ `Direct Connect 게이트웨이` 연결

### AMI와 EC2
= SQS와 항목수에 따라 

### `주식` + 여러 가용영역 블록 스토리지 + `저지연`
> iSCSI 프로토콜 사용 Multi-AZ 파일 시스템

### S3 Pll
= macie로 sns

### DB 반복적 + 읽기 용량 소모 
= DAX

### EC2 워크로드 + 노드간 통신
= 클러스터 배치 그룹 EC2 구성

### EFS 영구데이터 저장
= 매일 백업 시행, 다른리전에 복제 

### KMS
= 새 키 구성요소를 기존 KMS키로 가져옴 이전꺼는 만료시간 설정

### SDK 15개의 + 샤드 + 파티션키
= 파티션키를 서비스이름에서 생성 타임스탬프로 변경

### orcale 에서 aurora로 마이그레이션 암호화된 네트워크까지
= SCT,DMS 마이그레이션
= VPN 데이터 마이그레이션

### EMR
= 볼륨을 암호화하는 EMR 구성

### DB 데이터 장기분석
= s3 증분 방식

### 보고서 접근
= 미리 서명된 URL생성

### CLoud Trail 보안
= 로그파일 검증, KMS 활성화

### 반복적 크론 시
= shceduler 사용 및 corn 작업 
-> Lamdba아님


### 단일리전에서 글로벌 확장시
= 엑셀레이터

### 모니터링 = 그냥 모니터링

### 5000개 솔루션 + 500KB 
= Kinesis
-> 대용량 실시간 데이터 스트리밍을 수집·처리·분석하는 서비스

### EC2

네트워크 로드밸런서
자동크기조정그룹 

### ML나오면
= 무조건 `Lustre`

### 프록시
= RDS 개선
애플리케이션과 RDS 데이터베이스 사이에서 커넥션을 대신 관리해주는 중간 계층 서비스

### DNS는 
Rout53 연결 앤드포인트 생성

### AWS가 각 계정 +  사용 중앙관리 +  자동보안가드레일
= Control Tower => 계정 관련된거

### DNS 캐시 방지 + 두리전에 하나의 엔드포인트
= AWS Global Accelerator를 사용하여 애플리케이션 정석 애니캐스트 IP할당
- 애니캐스트 = DNS변경없이 ip할당


### EBS저장된 데이터 백업
= Flexible Retrival 스토리지 클래스

### CI/CD
= tjdrhdwjrdls 